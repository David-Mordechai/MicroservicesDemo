<Window x:Class="AeroMapPresentor.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:AeroMapPresentor.Infrastructure.ViewModels;assembly=AeroMapPresentor.Infrastructure"
        d:DataContext="{d:DesignInstance Type=viewModels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1200" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Image x:Name="MapImage" Grid.Row="0" Grid.Column="0" Source="{Binding ImageSource, TargetNullValue={x:Null}}" Stretch="Uniform" />
        <Canvas Grid.Row="0" Grid.Column="0" x:Name="CanvasEntities"/>
    </Grid>
</Window>
